name: bootstrap

#on:
  #workflow_dispatch:

jobs:
  bootstrap:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Bootstrap (frozen, manual only)
        run: |
          echo "=================================="
          echo "BOOTSTRAP WORKFLOW"
          echo "Status: FROZEN"
          echo "Trigger: manual (workflow_dispatch)"
          echo "----------------------------------"
          echo "This workflow is intentionally"
          echo "excluded from production pipeline."
          echo
          echo "If you are running this manually,"
          echo "you know exactly what you are doing."
          echo "=================================="

      # ↓↓↓
      # Ниже может быть ЛЮБОЙ старый bootstrap-код,
      # он сохранён намеренно.
      # Он НЕ запускается автоматически.
      # ↓↓↓

      # - name: Legacy bootstrap logic
      #   run: |
      #     echo "legacy bootstrap logic here"
