// package.json
{
  "name": "vpn-aggregator",
  "private": true,
  "type": "module",
  "scripts": {
    "pipeline": "node scripts/bootstrap.js && node scripts/pull_worker.js && node scripts/normalize.js && node scripts/filter.js && node scripts/score.js && node scripts/apply_stickiness.js && node scripts/history.js && node scripts/export_clash.js"
  },
  "dependencies": {
    "yaml": "^2.4.5"
  }
}
